///
/// HybridTtlockNitroSpecSwift.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#include "HybridTtlockNitroSpec.hpp"

// Forward declaration of `HybridTtlockNitroSpec_cxx` to properly resolve imports.
namespace TtlockNitro { class HybridTtlockNitroSpec_cxx; }

// Forward declaration of `NumberStringPair` to properly resolve imports.
namespace margelo::nitro::ttlocknitro { struct NumberStringPair; }
// Forward declaration of `DeviceSystemModal` to properly resolve imports.
namespace margelo::nitro::ttlocknitro { struct DeviceSystemModal; }
// Forward declaration of `InitGatewayParam` to properly resolve imports.
namespace margelo::nitro::ttlocknitro { struct InitGatewayParam; }
// Forward declaration of `InitGatewayModal` to properly resolve imports.
namespace margelo::nitro::ttlocknitro { struct InitGatewayModal; }
// Forward declaration of `InitLockParam` to properly resolve imports.
namespace margelo::nitro::ttlocknitro { struct InitLockParam; }
// Forward declaration of `NumberNumberPair` to properly resolve imports.
namespace margelo::nitro::ttlocknitro { struct NumberNumberPair; }
// Forward declaration of `NumberNumberNumberTriple` to properly resolve imports.
namespace margelo::nitro::ttlocknitro { struct NumberNumberNumberTriple; }
// Forward declaration of `CycleDateParam` to properly resolve imports.
namespace margelo::nitro::ttlocknitro { struct CycleDateParam; }
// Forward declaration of `NumberBooleanPair` to properly resolve imports.
namespace margelo::nitro::ttlocknitro { struct NumberBooleanPair; }
// Forward declaration of `StringNumberPair` to properly resolve imports.
namespace margelo::nitro::ttlocknitro { struct StringNumberPair; }
// Forward declaration of `WifiLockServerInfo` to properly resolve imports.
namespace margelo::nitro::ttlocknitro { struct WifiLockServerInfo; }
// Forward declaration of `AnyMap` to properly resolve imports.
namespace NitroModules { class AnyMap; }

#include <string>
#include "NumberStringPair.hpp"
#include <functional>
#include "DeviceSystemModal.hpp"
#include "InitGatewayParam.hpp"
#include <optional>
#include "InitGatewayModal.hpp"
#include "InitLockParam.hpp"
#include "NumberNumberPair.hpp"
#include "NumberNumberNumberTriple.hpp"
#include "CycleDateParam.hpp"
#include <vector>
#include "NumberBooleanPair.hpp"
#include "StringNumberPair.hpp"
#include "WifiLockServerInfo.hpp"
#include <NitroModules/AnyMap.hpp>

#include "TtlockNitro-Swift-Cxx-Umbrella.hpp"

namespace margelo::nitro::ttlocknitro {

  /**
   * The C++ part of HybridTtlockNitroSpec_cxx.swift.
   *
   * HybridTtlockNitroSpecSwift (C++) accesses HybridTtlockNitroSpec_cxx (Swift), and might
   * contain some additional bridging code for C++ <> Swift interop.
   *
   * Since this obviously introduces an overhead, I hope at some point in
   * the future, HybridTtlockNitroSpec_cxx can directly inherit from the C++ class HybridTtlockNitroSpec
   * to simplify the whole structure and memory management.
   */
  class HybridTtlockNitroSpecSwift: public virtual HybridTtlockNitroSpec {
  public:
    // Constructor from a Swift instance
    explicit HybridTtlockNitroSpecSwift(const TtlockNitro::HybridTtlockNitroSpec_cxx& swiftPart):
      HybridObject(HybridTtlockNitroSpec::TAG),
      _swiftPart(swiftPart) { }

  public:
    // Get the Swift part
    inline TtlockNitro::HybridTtlockNitroSpec_cxx& getSwiftPart() noexcept {
      return _swiftPart;
    }

  public:
    inline size_t getExternalMemorySize() noexcept override {
      return _swiftPart.getMemorySize();
    }
    void dispose() noexcept override {
      _swiftPart.dispose();
    }

  public:
    // Properties
    

  public:
    // Methods
    inline void startScanWirelessKeypad() override {
      auto __result = _swiftPart.startScanWirelessKeypad();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void stopScanWirelessKeypad() override {
      auto __result = _swiftPart.stopScanWirelessKeypad();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void initWirelessKeypad(const std::string& keypadMac, const std::string& lockMac, const std::function<void(const NumberStringPair& /* data */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.initWirelessKeypad(keypadMac, lockMac, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void startScanDoorSensor() override {
      auto __result = _swiftPart.startScanDoorSensor();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void stopScanDoorSensor() override {
      auto __result = _swiftPart.stopScanDoorSensor();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void initDoorSensor(const std::string& doorSensorMac, const std::string& lockData, const std::function<void(const NumberStringPair& /* data */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.initDoorSensor(doorSensorMac, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void startScanRemoteKey() override {
      auto __result = _swiftPart.startScanRemoteKey();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void stopScanRemoteKey() override {
      auto __result = _swiftPart.stopScanRemoteKey();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void initRemoteKey(const std::string& remoteMac, const std::string& lockData, const std::function<void(const NumberStringPair& /* data */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.initRemoteKey(remoteMac, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getRemoteKeySystemInfo(const std::string& remoteMac, const std::function<void(const DeviceSystemModal& /* data */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getRemoteKeySystemInfo(remoteMac, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void startScanGateway() override {
      auto __result = _swiftPart.startScanGateway();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void stopScanGateway() override {
      auto __result = _swiftPart.stopScanGateway();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void connect(const std::string& mac, const std::function<void(double /* state */)>& resolve) override {
      auto __result = _swiftPart.connect(mac, resolve);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getNearbyWifi(const std::function<void(double /* state */)>& resolve) override {
      auto __result = _swiftPart.getNearbyWifi(resolve);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void initGateway(const InitGatewayParam& params, const std::function<void(const InitGatewayModal& /* data */)>& resolve, const std::function<void(double /* errorCode */)>& reject) override {
      auto __result = _swiftPart.initGateway(std::forward<decltype(params)>(params), resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void startScan() override {
      auto __result = _swiftPart.startScan();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void stopScan() override {
      auto __result = _swiftPart.stopScan();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void initLock(const InitLockParam& params, const std::function<void(const std::string& /* lockData */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.initLock(std::forward<decltype(params)>(params), resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getLockVersionWithLockMac(const std::string& lockMac, const std::function<void(const std::string& /* lockVersion */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getLockVersionWithLockMac(lockMac, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getAccessoryElectricQuantity(double accessoryType, const std::string& accessoryMac, const std::string& lockData, const std::function<void(const NumberNumberPair& /* data */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getAccessoryElectricQuantity(std::forward<decltype(accessoryType)>(accessoryType), accessoryMac, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void resetLock(const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.resetLock(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void resetEkey(const std::string& lockData, const std::function<void(const std::string& /* lockData */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.resetEkey(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void controlLock(double controlAction, const std::string& lockData, const std::function<void(const NumberNumberNumberTriple& /* data */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.controlLock(std::forward<decltype(controlAction)>(controlAction), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void createCustomPasscode(const std::string& passcode, double startDate, double endDate, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.createCustomPasscode(passcode, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void recoverPasscode(const std::string& passcode, double passcodeType, double cycleType, double startDate, double endDate, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.recoverPasscode(passcode, std::forward<decltype(passcodeType)>(passcodeType), std::forward<decltype(cycleType)>(cycleType), std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void modifyPasscode(const std::string& passcodeOrigin, const std::string& passcodeNew, double startDate, double endDate, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.modifyPasscode(passcodeOrigin, passcodeNew, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void deletePasscode(const std::string& passcode, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.deletePasscode(passcode, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void resetPasscode(const std::string& lockData, const std::function<void(const std::string& /* lockData */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.resetPasscode(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getLockSwitchState(const std::string& lockData, const std::function<void(double /* state */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getLockSwitchState(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void addCard(const std::optional<std::vector<CycleDateParam>>& cycleList, double startDate, double endDate, const std::string& lockData, const std::function<void(const std::string& /* cardNumber */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.addCard(cycleList, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void recoverCard(const std::string& cardNumber, const std::optional<std::vector<CycleDateParam>>& cycleList, double startDate, double endDate, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.recoverCard(cardNumber, cycleList, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void modifyCardValidityPeriod(const std::string& cardNumber, const std::optional<std::vector<CycleDateParam>>& cycleList, double startDate, double endDate, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.modifyCardValidityPeriod(cardNumber, cycleList, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void deleteCard(const std::string& cardNumber, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.deleteCard(cardNumber, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void clearAllCards(const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.clearAllCards(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void addFingerprint(const std::optional<std::vector<CycleDateParam>>& cycleList, double startDate, double endDate, const std::string& lockData, const std::function<void(const std::string& /* fingerprintNumber */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.addFingerprint(cycleList, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void modifyFingerprintValidityPeriod(const std::string& fingerprintNumber, const std::optional<std::vector<CycleDateParam>>& cycleList, double startDate, double endDate, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.modifyFingerprintValidityPeriod(fingerprintNumber, cycleList, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void deleteFingerprint(const std::string& fingerprintNumber, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.deleteFingerprint(fingerprintNumber, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void clearAllFingerprints(const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.clearAllFingerprints(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void modifyAdminPasscode(const std::string& adminPasscode, const std::string& lockData, const std::function<void(const std::string& /* passcode */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.modifyAdminPasscode(adminPasscode, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void setLockTime(double timestamp, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.setLockTime(std::forward<decltype(timestamp)>(timestamp), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getLockTime(const std::string& lockData, const std::function<void(const std::string& /* lockTimestamp */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getLockTime(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getLockSystem(const std::string& lockData, const std::function<void(const DeviceSystemModal& /* systemInfo */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getLockSystem(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getLockElectricQuantity(const std::string& lockData, const std::function<void(double /* electricQuantity */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getLockElectricQuantity(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getLockOperationRecord(double type, const std::string& lockData, const std::function<void(const std::string& /* records */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getLockOperationRecord(std::forward<decltype(type)>(type), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getLockAutomaticLockingPeriodicTime(const std::string& lockData, const std::function<void(const NumberNumberNumberTriple& /* data */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getLockAutomaticLockingPeriodicTime(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void setLockAutomaticLockingPeriodicTime(double seconds, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.setLockAutomaticLockingPeriodicTime(std::forward<decltype(seconds)>(seconds), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getLockRemoteUnlockSwitchState(const std::string& lockData, const std::function<void(bool /* isOn */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getLockRemoteUnlockSwitchState(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void setLockRemoteUnlockSwitchState(bool isOn, const std::string& lockData, const std::function<void(const std::string& /* lockData */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.setLockRemoteUnlockSwitchState(std::forward<decltype(isOn)>(isOn), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getLockConfig(double config, const std::string& lockData, const std::function<void(const NumberBooleanPair& /* data */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getLockConfig(std::forward<decltype(config)>(config), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void setLockConfig(double config, bool isOn, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.setLockConfig(std::forward<decltype(config)>(config), std::forward<decltype(isOn)>(isOn), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void setLockSoundVolume(double soundVolumeValue, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.setLockSoundVolume(std::forward<decltype(soundVolumeValue)>(soundVolumeValue), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getLockSoundVolume(const std::string& lockData, const std::function<void(double /* soundVolumeValue */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getLockSoundVolume(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getUnlockDirection(const std::string& lockData, const std::function<void(double /* direction */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getUnlockDirection(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void setUnlockDirection(double direction, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.setUnlockDirection(std::forward<decltype(direction)>(direction), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void setUnlockDirectionAutomatic(const std::string& lockData, const std::function<void(double /* direction */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.setUnlockDirectionAutomatic(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void addPassageMode(double type, const std::optional<std::vector<double>>& weekly, const std::optional<std::vector<double>>& monthly, double startDate, double endDate, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.addPassageMode(std::forward<decltype(type)>(type), weekly, monthly, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void clearAllPassageModes(const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.clearAllPassageModes(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void addRemoteKey(const std::string& remoteMac, const std::optional<std::vector<CycleDateParam>>& cycleList, double startDate, double endDate, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.addRemoteKey(remoteMac, cycleList, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void modifyRemoteKey(const std::string& remoteMac, const std::optional<std::vector<CycleDateParam>>& cycleList, double startDate, double endDate, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.modifyRemoteKey(remoteMac, cycleList, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void deleteRemoteKey(const std::string& remoteMac, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.deleteRemoteKey(remoteMac, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void clearAllRemoteKey(const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.clearAllRemoteKey(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void addDoorSensor(const std::string& doorSensorMac, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.addDoorSensor(doorSensorMac, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void clearAllDoorSensor(const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.clearAllDoorSensor(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void setDoorSensorAlertTime(double time, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.setDoorSensorAlertTime(std::forward<decltype(time)>(time), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void scanWifi(const std::string& lockData, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.scanWifi(lockData, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void configWifi(const std::string& wifiName, const std::string& wifiPassword, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.configWifi(wifiName, wifiPassword, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void configServer(const std::string& ip, const std::string& port, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.configServer(ip, port, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getWifiInfo(const std::string& lockData, const std::function<void(const StringNumberPair& /* data */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getWifiInfo(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void configIp(const WifiLockServerInfo& info, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.configIp(std::forward<decltype(info)>(info), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getWifiPowerSavingTime(const std::string& lockData, const std::function<void(const std::string& /* powerSavingData */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.getWifiPowerSavingTime(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void configWifiPowerSavingTime(const std::optional<std::vector<double>>& days, double startDate, double endDate, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.configWifiPowerSavingTime(days, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void clearWifiPowerSavingTime(const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.clearWifiPowerSavingTime(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void addFace(const std::optional<std::vector<CycleDateParam>>& cycleList, double startDate, double endDate, const std::string& lockData, const std::function<void(const std::string& /* faceNumber */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.addFace(cycleList, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void addFaceFeatureData(const std::string& faceFeatureData, const std::optional<std::vector<CycleDateParam>>& cycleList, double startDate, double endDate, const std::string& lockData, const std::function<void(const std::string& /* faceNumber */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.addFaceFeatureData(faceFeatureData, cycleList, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void modifyFaceValidityPeriod(const std::optional<std::vector<CycleDateParam>>& cycleList, double startDate, double endDate, const std::string& faceNumber, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.modifyFaceValidityPeriod(cycleList, std::forward<decltype(startDate)>(startDate), std::forward<decltype(endDate)>(endDate), faceNumber, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void deleteFace(const std::string& faceNumber, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.deleteFace(faceNumber, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void clearFace(const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.clearFace(lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void activateLiftFloors(const std::string& floors, const std::string& lockData, const std::function<void(const NumberNumberNumberTriple& /* data */)>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.activateLiftFloors(floors, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void setLiftControlEnableFloors(const std::string& floors, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.setLiftControlEnableFloors(floors, lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void setLiftWorkMode(double workMode, const std::string& lockData, const std::function<void()>& resolve, const std::function<void(double /* errorCode */, const std::string& /* description */)>& reject) override {
      auto __result = _swiftPart.setLiftWorkMode(std::forward<decltype(workMode)>(workMode), lockData, resolve, reject);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void getBluetoothState(const std::function<void(double /* state */)>& resolve) override {
      auto __result = _swiftPart.getBluetoothState(resolve);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void supportFunction(double lockFunction, const std::string& lockData, const std::function<void(bool /* isSupport */)>& resolve) override {
      auto __result = _swiftPart.supportFunction(std::forward<decltype(lockFunction)>(lockFunction), lockData, resolve);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void addListener(const std::string& eventName, const std::function<void(const std::string& /* eventName */, const std::optional<std::shared_ptr<AnyMap>>& /* data */)>& listener) override {
      auto __result = _swiftPart.addListener(eventName, listener);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void removeListener(const std::string& eventName, const std::function<void(const std::string& /* eventName */, const std::optional<std::shared_ptr<AnyMap>>& /* data */)>& listener) override {
      auto __result = _swiftPart.removeListener(eventName, listener);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }

  private:
    TtlockNitro::HybridTtlockNitroSpec_cxx _swiftPart;
  };

} // namespace margelo::nitro::ttlocknitro
